---
"@dvashim/biome-config": patch
---

Bump Biome from 2.4.9 to 2.4.10. Update `$schema` URLs across all configs, `biome.json`, and README. Add two new nursery rules to `react-strict` and `react-balanced` configs: `noImpliedEval`, `noUnsafePlusOperands`.
