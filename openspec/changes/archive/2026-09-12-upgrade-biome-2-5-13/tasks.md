## 1. Advance the pinned target

- [x] 1.1 Confirm the starting state rather than assuming it: `pnpm exec biome --version` reports **2.5.13**, `package.json` already declares `^2.5.13`, and `pnpm run check` passes with `check:rule-metadata` **skipped**. If the binary is not 2.5.13, run `pnpm install` first — the metadata sweep refuses to run against a mismatched binary.
- [x] 1.2 Update the `$schema` URL from `2.5.11` to `2.5.13` in all six `dist/*.json` presets and in the root `biome.json`. Verify `grep -rl 'schemas/2\.5\.11/' dist/ biome.json` returns nothing.
- [x] 1.3 Update the seven `README.md` version references: the requirements-table `**2.5.11+**`, the `$schema` prose sentence, the two `biome.json` examples, the `@biomejs/biome@^2.5.11` install snippet, the `**Biome 2.5.11**` prose, and the remaining `schemas/2.5.11/schema.json` occurrence. Verify `grep -n '2\.5\.11' README.md` returns nothing, and that the historical "in Biome 2.5.0" mentions are **left alone** — they record when rules graduated, not the target.

## 2. Regenerate the rule metadata

- [x] 2.1 Run `pnpm sync-rule-metadata` with no `--biome` override, after 1.2. Verify it reports **538** rules at Biome 2.5.13 (524 + 14).
- [x] 2.2 Verify the snapshot diff is exactly the audit and nothing more: `biomeVersion`, plus 14 added entries, and **no** change to any existing rule's category, severity, recommended status, domains, or example languages. Confirm by diffing (`git diff audit/rule-metadata.json`), not by asserting — the "nothing graduated, nothing renamed, nothing removed" claim in `proposal.md` is what this diff has to show.
- [x] 2.3 Re-run the **per-hop** schema diff that the audit rests on, since this pass spans two releases: fetch `schemas/2.5.12/schema.json` as well and diff 2.5.11 → 2.5.12 → 2.5.13, not just the endpoints. Verify 8 added then 6 added, 0 removed and 0 category moves at **both** hops, rule totals 524 / 532 / 538, and that the union equals the 14-rule endpoint set — an endpoint-only diff cannot see a rule added in 2.5.12 and removed by 2.5.13.
- [x] 2.4 Verify the three excluded rules carry the domains the exclusion rests on: `noSvelteAtHtmlTags` → `svelte`, `noVueDeprecatedScopedSlots` → `vue`, `useVueBaseImport` → `vue`. Read them out of the regenerated snapshot, not from `biome explain`.

## 3. Add the eleven in-scope rules

- [x] 3.1 Add all 11 rules to `dist/biome.react-strict.json` under `nursery` at `"warn"`: `noBunModules`, `noInvalidFileInputAccept`, `noThisOutsideOfClass`, `noUnmodifiedLoopCondition`, `noUnsafeIframeSandbox`, `noXorAsExponentiation`, `useBetterDomTraversing`, `useFlatMathMinMax`, `useLayeredStyles`, `useModernMathApis`, `useReactNamingConvention`. Verify `nursery` then holds **94** entries.
- [x] 3.2 Add the same 11 to `dist/biome.react-balanced.json` under `nursery`, at `"warn"` except: `useBetterDomTraversing` → `"info"`, `useLayeredStyles` → `"off"`, `useReactNamingConvention` → `"info"`. Verify balanced's `nursery` holds **94** entries and that strict and balanced list the identical rule *set* — the parity invariant compares sets, not severities.
- [x] 3.3 Run `biome check --write` (**not** `biome format --write`) so the `useSortedKeys` assist places the new entries. All 11 are bare severity strings, so verify they land in the string-valued run at the front of `nursery`, ahead of the object-valued entries at its tail.
- [x] 3.4 Confirm the three excluded rules are **absent** from every preset and that **no** `audit/rule-exclusions.json` entry is added for any of them. Verify `pnpm run check:presets` reports **0 unaccounted** — the ledger rejects an entry the metadata already classifies, so a well-meant entry would fail the check.
- [x] 3.5 Run `pnpm sync-stable` and verify both `-stable` files change **only** in their `$schema` line: all 11 additions are nursery, so the stable totals stay at **180**. Verify `pnpm run check:sync-stable` reports no drift.

## 4. Reconcile the published counts

- [x] 4.1 Update the README ladder bullet `up to 263 explicitly configured rules` → **274**, and the `- **nursery** (83 rules)` per-category bullet → **94**. Both are read by a `check:presets` matcher; keep the surrounding wording intact so the matchers still anchor.
- [x] 4.2 Update the Configurations table: the `React strict` row to `274` / `94`, and the `React balanced` row to `274, 22 relaxed` / `94`. Leave both `-stable` rows at `180` and `180, 15 relaxed` — verify that is deliberate, not an oversight, and that `check:presets` agrees.
- [x] 4.3 Update the three checked relaxation phrases: `**19 targeted relaxations**` → **22**, `of the 19 relaxations` → **22**, and leave `those 15 apply in` and `with the 15 stable-category relaxations` at **15**. The leading word `Fifteen` stays correct and is prose, not a matcher.
- [x] 4.4 Add three rows to the README's balanced relaxation table, all under `nursery`: `useBetterDomTraversing` (warn → info, stylistic preference whose fixes are unsafe by the rule's own account), `useLayeredStyles` (warn → off, reports every style rule and `@import` outside a cascade layer, with no option to exempt), `useReactNamingConvention` (warn → info, naming convention that fires across an existing codebase on first adoption). Verify each name exists in `react-strict` — `check:presets` checks every rule the README names.
- [x] 4.5 Update the two sentences that enumerate the nursery relaxations to name **seven**, not four: the `> Fifteen of the 22 relaxations…` note, and the `react-balanced-stable` sentence's `(the four nursery relaxations drop out with their category)` → **seven**.
- [x] 4.6 Run `pnpm run check:presets` and confirm it passes, reconciling every count above at once. Do not verify any count by eye.

## 5. Update CLAUDE.md

- [x] 5.1 Update the four `CLAUDE.md` sentences the pass moves, correcting the two counts that were **already stale** before it: `82 of them nursery` → **94** (it was wrong at 83), `19 targeted relaxations` → **22**, `Only 15 of balanced's 19 relaxations` → **22**, and `the other four (…)` → **seven**, naming the three added rules alongside the existing four.
- [x] 5.2 Update the README-contract bullet's `the total (18)` → **22** — also already stale, against an actual 19. Verify by grep; nothing checks this file, which is why it is its own task rather than folded into section 4.

## 6. Record the upstream behaviour audit

- [x] 6.1 Re-run the fixture table from `proposal.md` against the installed 2.5.13 and confirm it still holds before the changeset repeats its claims: `useLayeredStyles` firing on a plain rule, a `:root` block, a CSS Module, and a bare `@import` but not inside `@layer`; `useReactNamingConvention` firing three times **with no React dependency present**; `useBetterDomTraversing` firing on the four traversal forms. Do not carry the verdict forward on the strength of the proposal.
- [x] 6.2 Re-run the `noBunModules` suppression test and confirm the partial gate: with `bun` and `bun-types` declared in a fixture `package.json`, `import { serve } from "bun"` is suppressed while `import { Database } from "bun:sqlite"` still reports. This is the evidence for the new requirement in `specs/linter-rule-coverage/spec.md`; if it no longer reproduces, the requirement and the `warn` decision both need revisiting.
- [x] 6.3 Record the upstream rule fixes by the audience each actually reaches. **Verification corrected the original list**: of the 13 rules fixed upstream, only **9** are recommended and domain-free and so reach all six presets — `noUnusedVariables` (declaration merging, merged namespaces, Unicode escapes in identifiers), `noUnusedPrivateClassMembers` (×3), `noUselessConstructor`, `useAriaPropsForRole`, `useFocusableInteractive`, `useHeadingContent`, `noDescendingSpecificity`, `noShorthandPropertyOverrides`, `noDuplicateProperties`. Three more are **not** recommended but are listed at `warn` in a stable category, so their fixes reach the four rule-listing presets (strict, balanced, and both `-stable`) and **not** `recommended` / `react-recommended`: `noInferrableTypes`, `useExplicitLengthCheck`, `noMisplacedAssertion`. The last, `noUnresolvedImports`, is listed at `"off"` in every preset and is not recommended, so its fix reaches **no** consumer of this package. Verified against the regenerated snapshot and the preset files, not asserted.
- [x] 6.4 Record the type-inference changes that surface **new diagnostics on unchanged consumer code** from already-listed rules — callback-parameter inference and namespace-import handling improving `noFloatingPromises`, `noMisusedPromises`, `noBaseToString`, `useNullishCoalescing`, `useRegexpExec`, `noUnnecessaryConditions`, `useExhaustiveSwitchCases` among others, plus the Zod-scale performance fix. Verify these rules are listed in `react-strict` before claiming consumer impact.
- [x] 6.5 Record the changes that reach files rather than rules: the formatter printing `declare` before accessibility modifiers, comments no longer moved next to a generic's `<`, CSS `url(@/…)` / `url(!…)` parsing and preserved escaped whitespace, the `useSortedAttributes` nested-JSX corruption fix, ~50% formatter performance, and the Astro/Svelte parser and formatter fixes. Verify the presets enable `html.experimentalFullSupportEnabled` in all six before claiming the Astro suppression-comment fix reaches consumers.
- [x] 6.6 Record the `useReactCompiler` changes — experimental capitalized-call and effect-dependency checks disabled, non-ASCII panic fixed. Verify it is listed at `warn` in `react-strict` and `off` in `react-balanced`, so the improvement reaches strict consumers only.

## 7. Release and verification

- [x] 7.1 Create a **minor** changeset — preset rule lists change. Lead with the type-inference and recommended-rule fixes that reach every preset without a rule-list move, then the 11 added rules and the three balanced relaxations with their grounds, then the asymmetry: strict/balanced 263 → 274 (nursery 83 → 94), both `-stable` unchanged at 180, relaxations 19 → 22 with the stable subset still 15.
- [x] 7.2 Run `pnpm run check` end to end and confirm all six checks pass — in particular that `check:rule-metadata` now **runs** instead of skipping, since the target and the binary are level, and that the pinned-target invariant agrees on 2.5.13 across the snapshot, seven `$schema` URLs, and the README.
- [x] 7.3 Confirm the `dist/*.json` diff is exactly what was intended: `git diff dist/` shows `$schema` lines in all six, plus 11 new nursery entries in each of strict and balanced and nothing else. No existing rule entry may move category or severity.
- [x] 7.4 Confirm the pass left the out-of-scope dev-tooling state alone: `git diff package.json` still shows only the `@biomejs/biome`, `@changesets/*`, `@types/node`, and `packageManager` lines that were already uncommitted, with no new edits from this change beyond what section 1 required.
- [x] 7.5 Run `openspec validate upgrade-biome-2-5-13 --strict` and confirm the change is valid before archiving.
