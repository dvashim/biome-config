# Tasks: track-latest-changes-1-10-2

## 1. Re-verify upstream state

- [x] 1.1 Confirm `@biomejs/biome` npm `latest` is still 2.5.3 (`npm view @biomejs/biome dist-tags`); if a newer stable exists, stop and revisit the proposal per the "Newer release available" scenario of `linter-rule-coverage`
- [x] 1.2 Confirm the other dev tools are still at npm `latest`: `validate-package-exports`, `@changesets/cli`, `@changesets/changelog-github`, `pnpm`; update any that fell behind and re-run `pnpm check`
- [x] 1.3 Record the Biome audit result in the PR description: presets already reconciled against 2.5.3, `noSvelteUnnecessaryStateWrap` (2.5.2/2.5.3's only new rule) is Svelte-only and out of scope, no dist changes (recorded in proposal.md and the commit body; PR text ready)

## 2. Update the OpenSpec CLI and regenerate assets

- [x] 2.1 Update the CLI: `pnpm add -g @fission-ai/openspec@latest`, then verify `openspec --version` reports 1.6.0 (or the current latest)
- [x] 2.2 Find the regeneration subcommand in 1.6.0 (`openspec --help`) and run it so `.claude/commands/opsx/*.md` and `.claude/skills/openspec-*/SKILL.md` are regenerated by the installed CLI (command: `openspec update`)
- [x] 2.3 Review the combined regen diff (pending 1.4.1→1.5.0 changes plus the new 1.5.0→1.6.0 changes); confirm every skill's `generatedBy` matches the installed version and no file was hand-edited
- [x] 2.4 Smoke-test the updated workflow: `openspec status --change track-latest-changes-1-10-2` and `openspec validate --changes` run cleanly

## 3. Land the change

- [x] 3.1 Run `pnpm check` and confirm formatting, exports, and stable-sync checks pass with no dist drift
- [x] 3.2 Confirm no changeset is needed (no `dist/`, `package.json`, or README changes); do not create one
- [x] 3.3 Commit the regenerated `.claude/` assets and the openspec change artifacts as one commit on the `up` branch
